# 闪卡记忆：完整学习流程图

## 一、主流程：用户学习全周期

```mermaid
flowchart TD
    Start([用户打开应用]) --> Init[初始化<br/>预加载记忆缓存<br/>加载词库]
    Init --> CheckTask{今日任务<br/>已开始?}
    CheckTask -->|否| BuildQueue[buildTodayQueue<br/>生成今日学习队列]
    CheckTask -->|是| Resume[恢复上次进度]
    BuildQueue --> Resume

    BuildQueue --> QueueSort[队列排序<br/>①到期词 按R升序<br/>②lapsed词 按遗忘次数降序<br/>③新词 按难度/随机]

    Resume --> MicroLoop

    subgraph MicroLoop [微循环核心]
        direction TB
        PickGroup[取一组词<br/>首组4词 / 后续5词<br/>优先取 sessionReviewQueue 重学词<br/>再取 todayQueue 到期词]
        PickGroup --> CheckEmpty{组内<br/>有词?}
        CheckEmpty -->|空| ExitLoop[退出微循环]
        CheckEmpty -->|有词| FlashcardPhase

        subgraph FlashcardPhase [闪卡学习阶段]
            direction TB
            ShowCard[显示闪卡正面<br/>英文 + 音标]
            ShowCard --> UserClick{用户操作}
            UserClick -->|点击发音| PlayTTS[播放 TTS 读音] --> ShowCard
            UserClick -->|点击翻转| ShowBack[显示背面<br/>中文 + 例句 + 记忆法]
            ShowBack --> SelfRate{用户自评}
            SelfRate -->|认识| RateKnown[rate word, known<br/>grade = 3 或 4]
            SelfRate -->|模糊| RateFuzzy[rate word, fuzzy<br/>grade = 2]
            SelfRate -->|不认识| RateUnknown[rate word, unknown<br/>grade = 1]
            RateKnown --> FSRSUpdate[FSRS 更新 D/S/R<br/>计算 nextReview<br/>立即写入 localStorage]
            RateFuzzy --> FSRSUpdate
            RateUnknown --> FSRSUpdate
            FSRSUpdate --> CheckKnown{grade ≥ 3?}
            CheckKnown -->|是| MarkDone[标记今日已完成<br/>移出 sessionReviewQueue]
            CheckKnown -->|否| AddQueue[加入 sessionReviewQueue<br/>等待下组重学]
            MarkDone --> NextCard
            AddQueue --> NextCard[下一张闪卡]
            NextCard --> CardEnd{本组闪卡<br/>全部完成?}
            CardEnd -->|否| ShowCard
            CardEnd -->|是| GamePhase
        end

        subgraph GamePhase [消消乐检验阶段]
            direction TB
            StartGame[开始游戏<br/>动态时长 = pairCount × 8<br/>范围 20~90 秒]
            StartGame --> ShowBoard[显示中英文方块<br/>打乱排列]
            ShowBoard --> UserMatch{用户点击<br/>两个方块}
            UserMatch --> MatchCheck{是否匹配?}
            MatchCheck -->|正确| RateSuccess[rate word, matchSuccess<br/>grade = 3]
            MatchCheck -->|错误| RateFail[rate word, matchFail<br/>grade = 1]
            RateSuccess --> FSRSGame[FSRS 更新 D/S/R<br/>立即写入]
            RateFail --> FSRSGame
            RateSuccess --> RemovePair[消除方块<br/>得分 + 连击 + 音效]
            RateFail --> Shake[抖动 + 显示正确释义<br/>加入 sessionReviewQueue]
            RemovePair --> GameProgress{全部<br/>消除?}
            Shake --> GameProgress
            GameProgress -->|否| CheckTime{时间<br/>到?}
            GameProgress -->|是| GameEnd[游戏结束]
            CheckTime -->|未到| ShowBoard
            CheckTime -->|超时| RateTimeout[未配对词<br/>rate word, matchTimeout<br/>grade = 2<br/>加入 sessionReviewQueue]
            RateTimeout --> GameEnd
        end

        GameEnd --> NextGroup[取下一组词]
        NextGroup --> PickGroup
    end

    ExitLoop --> Completion
    NextGroup --> PickGroup

    subgraph Completion [结束报告]
        direction TB
        ShowReport[显示学习报告]
        ShowReport --> ReportContent[每词评分徽章<br/>游戏结果图标 ✓ ✗ ⏱<br/>下次复习时间<br/>游戏得分 + 星星]
        ReportContent --> CoinReward[发放金币奖励<br/>认识×2 + 模糊×1]
        CoinReward --> SessionEnd([本次学习结束])
    end

    SessionEnd --> WaitNext[等待 FSRS 调度]
    WaitNext --> DueCheck{有词 R < 0.9<br/>到期?}
    DueCheck -->|是| MicroLoop
    DueCheck -->|否| WaitNext
```

---

## 二、FSRS 算法内部流程

```mermaid
flowchart TD
    Input([调用 rate word, outcome]) --> LoadMem[加载该词记忆数据<br/>从内存 Map 缓存]
    LoadMem --> CheckVer{algorithmVersion<br/>< 2?}
    CheckVer -->|是 旧数据| Migrate[_migrateToFSRS<br/>估算 S: 由 familiarity 映射<br/>估算 D: 由 lapseCount/wrongCount<br/>估算 lastGrade]
    CheckVer -->|否 已是V2| Skip[直接使用]
    Migrate --> MapGrade
    Skip --> MapGrade

    MapGrade[outcomeToGrade<br/>映射为 1-4 评分]
    MapGrade --> GradeTable

    subgraph GradeTable [评分映射表]
        direction LR
        G1[known 连续正确≥3 → grade 4]
        G2[known → grade 3]
        G3[fuzzy → grade 2]
        G4[unknown → grade 1]
        G5[matchSuccess → grade 3]
        G6[matchFail → grade 1]
        G7[matchTimeout → grade 2]
    end

    GradeTable --> CalcR[计算当前可回忆性 R<br/>R = 1 + t/9S ^ -1<br/>t = 距上次复习天数]
    CalcR --> CheckFirst{首次<br/>复习?<br/>reviewCount = 0}
    CheckFirst -->|是| InitSD[初始化<br/>S = initStability grade<br/>D = initDifficulty - grade-3 × 0.94]
    CheckFirst -->|否| UpdateD[更新难度<br/>D = D + grade-3 × 0.1<br/>- D-Dinit × 0.1<br/>范围 1~10]

    InitSD --> CheckGrade
    UpdateD --> CheckGrade{grade<br/>= 1?}

    CheckGrade -->|是 失败| UpdateSFail[更新稳定性 失败<br/>S = 0.05 × D^-0.34<br/>× S+1^1.26 - 1<br/>× e^0.29×1-R]
    CheckGrade -->|否 成功| UpdateSSuccess[更新稳定性 成功<br/>S = S × 1 + e^0.86 × 11-D<br/>× S^-0.01 × e^1.49×1-R - 1<br/>grade=2: ×0.94<br/>grade=4: ×2.18]

    UpdateSFail --> CalcInterval
    UpdateSSuccess --> CalcInterval

    CalcInterval[计算复习间隔<br/>interval = S × R^−1/0.2 - 1<br/>目标 R = 0.9]
    CalcInterval --> MinInterval[应用最小间隔<br/>grade=1: 至少 1 分钟<br/>grade=2: 至少 10 分钟]
    MinInterval --> UpdateFields[更新全部字段]

    subgraph UpdateFields [更新后的记忆数据]
        direction TB
        F1[stability = 新 S]
        F2[difficulty = 新 D]
        F3[lastGrade = 本次 grade]
        F4[retrievability = 新 S 对应的 R]
        F5[algorithmVersion = 2]
        F6[reviewCount + 1]
        F7[lastReview = 当前时间]
        F8[nextReview = 当前时间 + interval]
        F9[familiarity = 由 S/D 映射 0-5]
        F10[status = new/learning/mastered/lapsed]
        F11[consecutiveCorrect 连续正确≥3则+1 否则清零]
    end

    UpdateFields --> Save[saveWordMemory<br/>立即写入 localStorage<br/>同步更新内存缓存]
    Save --> Return([返回更新后的记忆数据])
```

---

## 三、单词状态机

```mermaid
stateDiagram-v2
    [*] --> new: 词库加载

    new --> learning: 首次 rate 调用<br/>reviewCount 0→1

    learning --> learning: grade = 2 模糊<br/>S 缓慢增长

    learning --> mastered: grade ≥ 3<br/>且 S ≥ 7

    learning --> lapsed: grade = 1<br/>回忆失败

    mastered --> learning: grade = 1<br/>S 大幅下降<br/>consecutiveCorrect 清零

    mastered --> mastered: grade ≥ 3<br/>S 继续增长<br/>间隔越来越长

    lapsed --> learning: 重新学习<br/>grade ≥ 2

    lapsed --> lapsed: grade = 1<br/>继续遗忘

    mastered --> [*]: S ≥ 30<br/>长期稳定

    note right of learning
        S 范围: 0.1 ~ 7
        复习间隔: 分钟级 ~ 天级
    end note

    note right of mastered
        S 范围: 7 ~ 30+
        复习间隔: 天级 ~ 月级
    end note

    note right of lapsed
        S 重置为低值
        D 难度上升
        复习频繁
    end note
```

---

## 四、数据流转图

```mermaid
flowchart LR
    subgraph 用户层
        U[用户操作<br/>自评 / 配对 / 超时]
    end

    subgraph 接口层
        R[MemorySystem.rate<br/>统一入口]
    end

    subgraph 算法层
        F[FSRS-4 引擎<br/>D/S/R 三因子模型]
    end

    subgraph 存储层
        LS[(localStorage<br/>wordmatch_mem_ 前缀)]
        MC[内存 Map 缓存<br/>_memoryCache]
    end

    subgraph 调度层
        TQ[todayQueue<br/>今日学习队列]
        SRQ[sessionReviewQueue<br/>本轮重学队列]
        CT[completedToday<br/>已完成集合]
    end

    subgraph 展示层
        FC[闪卡界面]
        GB[游戏界面]
        CP[结束报告]
    end

    U -->|outcome| R
    R -->|grade| F
    F -->|D/S/R/nextReview| R
    R -->|写入| MC
    MC -->|持久化| LS
    MC -->|读取| R

    LS -->|buildTodayQueue| TQ
    TQ -->|取词| FC
    SRQ -->|优先取词| FC
    CT -->|过滤已完成| TQ

    FC -->|学习完成| GB
    GB -->|游戏完成| CP
    FC -->|fuzzy/unknown| SRQ
    GB -->|matchFail/timeout| SRQ
    FC -->|known| CT
    GB -->|matchSuccess| CT

    CP -->|展示 nextReview| U
```

---

## 五、微循环时序图

```mermaid
sequenceDiagram
    participant U as 用户
    participant S as 系统
    participant FSRS as FSRS引擎
    participant LS as localStorage

    Note over U,LS: 第 1 组 (4个新词)

    U->>S: 开始学习
    S->>FSRS: buildTodayQueue
    FSRS->>LS: 读取所有词记忆数据
    LS-->>FSRS: 返回缓存
    FSRS-->>S: 排序后的今日队列

    loop 4 张闪卡
        U->>S: 翻转卡片
        S-->>U: 显示中文+例句
        U->>S: 自评 "认识"
        S->>FSRS: rate(word, "known")
        FSRS->>FSRS: 更新 D/S/R
        FSRS->>LS: 立即写入
        FSRS-->>S: 返回新记忆数据
        S-->>U: 下一张卡片
    end

    Note over U,LS: 消消乐游戏

    S-->>U: 显示4对中英文方块 (32秒)
    
    loop 配对
        U->>S: 点击两个方块
        alt 匹配正确
            S->>FSRS: rate(word, "matchSuccess")
            FSRS->>LS: 立即写入
            S-->>U: 消除+得分+连击
        else 匹配错误
            S->>FSRS: rate(word, "matchFail")
            FSRS->>LS: 立即写入
            S->>S: 加入 sessionReviewQueue
            S-->>U: 抖动+显示正确答案
        end
    end

    Note over U,LS: 第 2 组 (5词: 1重学 + 4新词)

    S->>S: 从 sessionReviewQueue 取重学词
    S->>S: 从 todayQueue 取到期词
    S-->>U: 开始第2组闪卡

    Note over U,LS: ... 循环直至队列清空 ...

    S-->>U: 显示结束报告
    S-->>U: 每词评分 + 下次复习时间
    S-->>U: 游戏得分 + 星星 + 金币
```

---

## 六、复习调度决策图

```mermaid
flowchart TD
    OpenApp([用户打开应用]) --> LoadAll[加载所有词的记忆数据]
    LoadAll --> Scan[扫描每个词]
    Scan --> CheckDue{nextReview<br/>≤ 现在?}
    CheckDue -->|是| CalcR[计算 R = 1+t/9S ^ -1]
    CheckDue -->|否| Skip[跳过]

    CalcR --> AddDue[加入 dueReviews<br/>按 R 升序排列<br/>R 最低 = 最紧急]
    AddDue --> CheckLapsed{status<br/>= lapsed?}
    CheckLapsed -->|是| AddLapsed[加入 lapsedWords<br/>按遗忘次数降序]
    CheckLapsed -->|否| NextWord[检查下一个词]
    Skip --> NextWord
    NextWord --> Scan

    Scan --> CheckNew{reviewCount<br/>= 0?}
    CheckNew -->|是| AddNew[加入 newWords<br/>取 dailyNewWords 个]
    CheckNew -->|否| Done[扫描完成]

    AddLapsed --> Done
    AddNew --> Done

    Done --> Merge[合并队列<br/>① dueReviews R升序<br/>② lapsedWords 遗忘次数降序<br/>③ newWords 难度/随机]
    Merge --> TodayQueue[今日学习队列]
    TodayQueue --> MicroLoop[进入微循环学习]
```

---

## 七、关键路径总结

| 阶段 | 用户行为 | 系统响应 | FSRS 更新 | 数据写入 |
|------|---------|---------|----------|---------|
| 闪卡-认识 | 点击"认识" | 下一张卡片 | grade 3/4 → S 增长 | 立即写入 |
| 闪卡-模糊 | 点击"模糊" | 下一张卡片 + 加入重学队列 | grade 2 → S 缓增 | 立即写入 |
| 闪卡-不会 | 点击"不认识" | 显示记忆法 + 加入重学队列 | grade 1 → S 重置 | 立即写入 |
| 游戏-配对成功 | 点击匹配对 | 消除+得分+连击 | grade 3 → S 增长 | 立即写入 |
| 游戏-配对失败 | 点击错误对 | 抖动+显示答案+加入重学 | grade 1 → S 重置 | 立即写入 |
| 游戏-超时 | 时间到 | 未配对词自动评分 | grade 2 → S 缓降 | 立即写入 |
| 组间切换 | 自动 | 取下一组(含重学词) | — | — |
| 全部完成 | 自动 | 显示结束报告 | — | 批量发放金币 |
| 到期复习 | 打开应用 | 生成复习队列 | R < 0.9 的词优先 | — |
