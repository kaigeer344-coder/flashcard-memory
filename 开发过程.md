# 闪卡记忆应用 - 开发过程文档

> 本文档记录「闪卡记忆」单文件 HTML 应用从基础功能完善到商业化上线的完整开发历程。
> 主文件：`闪卡记忆.html`（约 15,600+ 行，含内联 CSS 和 JS）

---

## 一、项目概述

- **应用类型**：单文件 HTML 闪卡记忆背单词应用
- **目标用户**：英语学习者（CET4 / CET6 / 考研 / 雅思 / TOEFL）
- **核心功能**：闪卡学习 + 连连看游戏复习 + 间隔重复记忆算法
- **技术栈**：纯前端 HTML + CSS + JavaScript + localStorage 持久化

---

## 二、部署架构演进

### 2.1 从 GitHub Pages 迁移到腾讯云 CloudBase

**问题**：GitHub Pages 在国内访问缓慢甚至无法打开，影响用户体验。

**方案**：迁移到腾讯云 CloudBase 2.0 静态托管，国内用户直连无障碍。

**环境信息**：
- EnvId：`project123-carl-d5f16u165be3d97a`
- 访问地址：`https://project123-carl-d5f16u165be3d97a-1341005541.tcloudbaseapp.com`
- CLI 路径：`/Users/adminmima0000/.node/lib/node_modules/@cloudbase/cli/bin/`
- 认证方式：JWT 格式 API Key（`--cloudbase-api-key`）

### 2.2 部署流程

1. 准备临时部署目录 `/tmp/cloudbase-deploy`，只含网站文件（避免上传 .git / .github）
2. 执行 `tcb login --cloudbase-api-key "<JWT>" -e <envId>` 登录
3. 执行 `tcb hosting deploy <dir> -e <envId>` 部署
4. CDN 缓存约几分钟内刷新

### 2.3 CI/CD

配置了 GitHub Actions（`.github/workflows/deploy.yml`），push 到 main 分支自动触发部署。 secrets 需配置 `TCB_API_KEY` 和 `TCB_ENV_ID`。

---

## 三、UI 调整历程

### 3.1 完成页加「返回首页」按钮

在今日学习任务完成后，增加返回首页的入口，避免用户卡在完成页。

### 3.2 Header 精简

**问题**：顶部 Header 同时显示等级 / 金币 / 宝石 / 今日新词量，过于拥挤。

**方案**：
- 移除 Header 中的等级、金币、宝石显示
- 这些信息统一收纳到「我的」（个人中心）页
- Header 只保留「今日新词量」+「个人中心」入口

**最终结构**：
```html
<div class="game-header">
    <h1><img src="assets/word-match-logo.png" alt="">闪卡记忆</h1>
    <div class="header-actions">
        <div class="stat-item stat-new-words" id="headerNewWords">📚 10</div>
        <button class="header-settings-btn" onclick="switchTab('profile')">👤</button>
    </div>
</div>
```

### 3.3 底部导航改造

参考微信「我的 + 设置」逻辑：
- 原「设置」tab 改为「我的」（个人中心主页）
- 「设置」变成「我的」下的子页

**最终导航**：
```html
<nav class="bottom-nav">
    <button class="nav-item active" data-tab="home">🏠 首页</button>
    <button class="nav-item" data-tab="stats">📊 统计</button>
    <button class="nav-item" data-tab="profile">👤 我的</button>
</nav>
```

`switchTab()` 多级导航支持：profile → settings / vip 子页时，底部导航高亮仍保留在 profile。

### 3.4 配置项分离

**问题**：原本「今日新词数」和「每组单词数」混在一起，用户容易混淆。

**方案**：彻底分离两个配置项，各自独立 load/save。

| 配置项 | 位置 | 范围 | 操作变量 |
|--------|------|------|----------|
| 今日学习新词数 | 首页 | 5-50 | `state.dailyNewWords` |
| 每组单词数 | 设置子页 | 5-20 | `state.wordsPerRound` |

### 3.5 Header 元素位置调整

将「📚 今日新词量」从中间移到右侧，紧贴 👤 头像按钮，视觉更平衡。

---

## 四、商业化开发 - 会员订阅闭环

### 4.0 需求确认

- **不需要登录注册功能**（纯前端 + localStorage 模拟）
- **定价**：月卡 ¥9.9（主推）/ 季卡 ¥25.9 / 年卡 ¥88
- **Demo 性质**：「不一定需要都是真的，可以只是有真的一个功能能点击就行」
- 分 6 个步骤实施

### 4.1 步骤 1：VIP 状态管理 ✅

**目的**：建立会员状态的数据层基础。

**实现内容**：

新增常量与状态变量：
```javascript
const VIP_KEY = 'wordmatch_vip';
const VIP_PLANS = {
    monthly:   { id: 'monthly',   name: '月卡', price: 9.9,  durationDays: 30,  unit: '/月',   label: '主推',   featured: true  },
    quarterly: { id: 'quarterly', name: '季卡', price: 25.9, durationDays: 90,  unit: '/季',   label: '省3.8元', featured: false },
    yearly:    { id: 'yearly',    name: '年卡', price: 88,   durationDays: 365, unit: '/年',   label: '省30.8元', featured: false }
};
const FREE_LEVELS = ['cet4', 'cet6'];
const VIP_LEVELS = ['toefl'];
const FREE_DAILY_MAX = 20;
const VIP_DAILY_MAX = 50;
```

核心函数：
- `loadVipState()` / `saveVipState()` - localStorage 持久化
- `isVip()` - 判断当前是否为有效会员
- `getVipExpireDate()` / `getVipPlanName()` - 获取到期日期 / 套餐名
- `activateVip(planId)` - **续费累加逻辑**（有效会员在原 expireAt 基础上累加，不覆盖）
- `checkVipExpired()` - 过期自动清零

`DOMContentLoaded` 中调用 `loadVipState()`。

### 4.2 步骤 2：会员中心页 UI ✅

**目的**：构建会员中心展示页。

**HTML 结构**：新增 `vipScreen`，包含：
- 顶部金色渐变会员卡片 `vip-hero`（动态切换「开通会员」/「会员尊享」）
- 会员权益对比表（6 行：词库范围 / 每日新词 / 道具 / 统计 / 去广告等）
- 套餐选择区 `vip-plans`（3 列网格，月卡居中放大）

**CSS 样式**：
- `.vip-hero` - 金色渐变 `linear-gradient(135deg, #FFD700 0%, #FFA500 100%)`
- `.vip-plan-card.featured` - 主推套餐金色边框 + 阴影
- `.vip-plan-label` - 浮动「主推」标签
- 响应式：380px 以下套餐卡片堆叠为单列

**renderVipScreen() 函数**：根据 `isVip()` 切换 hero 文案，渲染 3 个套餐卡片（onclick 绑定 `openPay('${p.id}')`）。

### 4.3 步骤 3：支付页 + 模拟支付流程 ✅

**目的**：实现可点击的完整支付闭环。

**HTML 结构**：新增 `payOverlay` 模态层，三态切换：
1. **订单表单**：套餐名 + 价格 + 时长 + 微信/支付宝切换 + 确认按钮
2. **Loading 状态**：CSS 转圈动画 + 文案「正在唤起微信支付…」
3. **成功状态**：绿色 ✓ 弹出动画 + 「支付成功」+ 完成按钮

**CSS 关键动画**：
```css
@keyframes paySpin { to { transform: rotate(360deg); } }  /* loading 转圈 */
@keyframes paySuccessPop {  /* 成功图标弹出 */
    0%   { transform: scale(0); opacity: 0; }
    60%  { transform: scale(1.15); opacity: 1; }
    100% { transform: scale(1); }
}
```

**JS 函数**：
- `openPay(planId)` - 打开支付页，填入套餐信息，重置三态
- `selectPayMethod(method)` - 切换微信/支付宝（radio 样式）
- `confirmPay()` - 触发支付流程：禁用按钮 → 显示 loading → setTimeout 2000ms → `activateVip()` → 显示成功动画
- `closePayAndRefresh()` - 关闭支付页 + 刷新 VIP 页 / 个人中心 / 词库锁定状态

**支付流程时序**：
```
点击套餐 → openPay(plan) → 填订单信息 → 用户选支付方式
→ confirmPay() → 显示 loading → 2 秒后 activateVip(plan)
→ 显示成功动画 → 用户点完成 → closePayAndRefresh() 刷新界面
```

**防重复点击**：`_payProcessing` 标志位，处理中禁用按钮 + 拦截关闭。

### 4.4 步骤 4：权益限制（词库锁弹窗） ✅

**目的**：让 VIP 权益真正生效，非会员无法使用托福词库。

**词库卡片改造**：
- 托福卡片加 `.vip-locked` class
- 右上角显示金色 `👑 VIP` 徽标
- 会员开通后徽标自动变绿色 `✓ VIP`（通过 `.unlocked` class）

**selectLevel() 拦截**：
```javascript
function selectLevel(el) {
    const level = el.dataset.level;
    // VIP 词库拦截：非会员不能选 toefl
    if (VIP_LEVELS.indexOf(level) >= 0 && !isVip()) {
        const names = { toefl: '托福词汇' };
        showVipLock(names[level] || '该词库');
        return;  // 不切换 state.level
    }
    // ... 原有切换逻辑
}
```

**vipLockOverlay 弹窗**：
- 🔒 图标 + 「会员专享词库」标题 + 词库名
- 「开通会员 ¥9.9/月起」金色按钮 → 跳转会员中心
- 「稍后再说」取消按钮

**辅助函数**：
- `showVipLock(levelName)` - 显示弹窗
- `closeVipLock()` - 关闭弹窗
- `goVipFromLock()` - 关闭弹窗 + 跳转会员中心
- `refreshLevelLockState()` - 根据 VIP 状态刷新所有词库卡片的锁定视觉

`refreshLevelLockState()` 在三处调用：DOMContentLoaded 初始化、`closePayAndRefresh()` 支付成功后、会员状态变化时。

### 4.5 步骤 5：个人中心会员入口 ✅

**目的**：在「我的」页提供显眼的会员入口。

**HTML**：在 `profile-card` 下方新增 `profile-vip-card`：
```html
<div class="profile-vip-card" id="profileVipCard" onclick="switchTab('vip')">
    <div class="profile-vip-icon">👑</div>
    <div class="profile-vip-text">
        <div class="profile-vip-title" id="profileVipTitle">开通会员</div>
        <div class="profile-vip-sub" id="profileVipSub">解锁全部词库 · 享无限道具</div>
    </div>
    <div class="profile-vip-arrow">›</div>
</div>
```

**CSS 双态样式**：
- 未开通：金色渐变背景 `linear-gradient(135deg, #FFD700 0%, #FFA500 100%)`
- 已开通：深棕渐变背景 `linear-gradient(135deg, #5D4037 0%, #3E2723 100%)`（通过 `.activated` class）

**renderProfileScreen() 更新**：
```javascript
if (isVip()) {
    vipCard.classList.add('activated');
    vipTitle.textContent = '👑 会员尊享';
    vipSub.textContent = `${getVipPlanName()} · 到期 ${getVipExpireDate()}`;
} else {
    vipCard.classList.remove('activated');
    vipTitle.textContent = '开通会员';
    vipSub.textContent = '解锁全部词库 · 享无限道具 · ¥9.9/月起';
}
```

### 4.6 步骤 6：每日新词数限制 + 部署测试 ✅

**目的**：非会员每日新词上限 20，会员上限 50。

**changeDailyNewWords() 改造**：
```javascript
function changeDailyNewWords(delta) {
    const vipLimit = isVip() ? VIP_DAILY_MAX : FREE_DAILY_MAX;
    const max = Math.min(vipLimit, wordDB[state.level].length);
    // 非会员尝试突破上限时，提示开通会员
    if (!isVip() && delta > 0 && state.dailyNewWords >= FREE_DAILY_MAX) {
        showVipLock('每日 50 词上限');
        return;
    }
    state.dailyNewWords = Math.max(5, Math.min(max, state.dailyNewWords + delta));
    // ...
}
```

**loadDailyNewWords() 兜底**：自动夹紧历史值，避免老用户残留 50 词配置绕过限制。

**初始化顺序调整**：`loadVipState()` 必须在 `loadDailyNewWords()` 之前执行，确保 VIP 状态已知。

**部署测试**：通过 `tcb hosting deploy` 部署到 CloudBase，7 个文件全部上传成功。

---

## 五、关键技术点总结

### 5.1 续费累加逻辑

```javascript
function activateVip(planId) {
    const plan = VIP_PLANS[planId];
    const now = Date.now();
    const durationMs = plan.durationDays * 24 * 60 * 60 * 1000;
    // 续费：有效会员在原 expireAt 基础上累加；否则从当前时间开始算
    const baseTime = isVip() ? _vipState.expireAt : now;
    _vipState = { isVip: true, plan: planId, activatedAt: now, expireAt: baseTime + durationMs };
    saveVipState();
}
```

### 5.2 模态层复用模式

所有弹窗统一使用 overlay 模式：
- `.xxx-overlay` 默认 `display: none`
- `.xxx-overlay.show` → `display: flex`（居中）
- 半透明背景 + `backdrop-filter: blur()` 毛玻璃效果
- 卡片有入场动画（`@keyframes payCardIn`）

### 5.3 多级页面导航

`switchTab()` 支持主 tab 和子页：
- 主 tab（home / stats / profile）：切换时高亮对应底部导航
- 子页（settings / vip）：底部导航高亮保留在 profile，子页通过返回按钮回到 profile

### 5.4 VIP 权益拦截点

共 3 处拦截：
1. **词库选择**：`selectLevel()` 拦截托福
2. **每日新词上限**：`changeDailyNewWords()` 拦截突破 20 词
3. **未来可扩展**：道具数量、去广告等

---

## 六、遇到的问题和解决方案

### 6.1 CloudBase CLI 登录态过期

**问题**：部署时提示 `No valid identity information, please use cloudbase login to login`。

**解决**：每次部署前重新执行 `tcb login --cloudbase-api-key "<JWT>" -e <envId>`。

### 6.2 Sandbox 限制写日志

**问题**：`tcb hosting deploy` 返回 exit code 1，错误信息 `TRAE Sandbox Error: hit restricted Not allow operate files: /Users/adminmima0000/cloudbase-framework/logs/...`。

**解决**：文件实际已上传成功，只是 CLI 无法写日志文件。不影响部署结果，可忽略。

### 6.3 CloudBase CLI 不在 PATH 中

**问题**：`tcb` 命令找不到。

**解决**：使用绝对路径 `export PATH="/Users/adminmima0000/.node/lib/node_modules/@cloudbase/cli/bin:$PATH"`。

### 6.4 CDN 缓存不刷新

**问题**：部署后浏览器看到的还是旧版本。

**解决**：
- 使用无痕模式打开
- 或 `curl -H "Cache-Control: no-cache" <url>` 强制刷新
- CDN 通常几分钟内自动刷新

### 6.5 GitHub push 超时

**问题**：代理 127.0.0.1:7890 未运行导致 `git push` 超时。

**解决**：改用本地 CLI 直接部署 CloudBase 作为替代方案（CI/CD 配置好后 push 自动触发部署）。

---

## 七、文件结构

```
trae/
├── 闪卡记忆.html          # 主应用文件（约 15,600+ 行）
├── index.html             # 入口跳转页
├── assets/
│   ├── word-match-logo.png
│   ├── icon-cet4.png
│   ├── icon-cet6.png
│   └── icon-toefl.png
├── .github/
│   └── workflows/
│       └── deploy.yml     # GitHub Actions CI/CD
├── .gitignore
└── 开发过程.md            # 本文档
```

---

## 八、线上访问

- **访问地址**：[https://project123-carl-d5f16u165be3d97a-1341005541.tcloudbaseapp.com/](https://project123-carl-d5f16u165be3d97a-1341005541.tcloudbaseapp.com/)
- **部署方式**：腾讯云 CloudBase 2.0 静态托管
- **CI/CD**：push 到 GitHub main 分支自动部署

---

## 九、完整会员订阅闭环验证流程

1. 打开应用 → 首页可见「今日学习新词数」上限 20
2. 点击托福词库 → 弹出 VIP 锁弹窗
3. 点「开通会员」→ 跳转会员中心
4. 选择月卡（¥9.9）→ 打开支付页
5. 选择微信/支付宝 → 点「确认支付」
6. Loading 转圈 2 秒 → 显示绿色 ✓ 成功动画
7. 点「完成」→ 返回会员中心，显示「会员尊享 + 到期日期」
8. 回到首页 → 托福徽标变绿 ✓，可正常选择
9. 今日新词数可调到 50
10. 个人中心顶部显示「👑 会员尊享」深色卡片

闭环完成。
