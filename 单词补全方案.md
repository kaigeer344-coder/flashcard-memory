# 单词补全方案

## 一、现状分析

| 词库 | 当前词数 | 标准词数 | 完成度 | 缺口 |
|------|---------|---------|--------|------|
| CET4 | 4,412 | ~4,500 | 98% | ~88 |
| CET6 | 5,506 | ~6,000 | 92% | ~494 |
| TOEFL | **103** | ~4,000 | **2.6%** | **~3,897** |
| GRE | **108** | ~3,000 | **3.6%** | **~2,892** |
| **合计** | **10,129** | **~17,500** | **58%** | **~7,371** |

**核心问题**：TOEFL 和 GRE 只是演示样本（各约 100 词），不是真正的词库。

## 二、数据源选择

### 方案对比

| 数据源 | 优点 | 缺点 | 适合度 |
|--------|------|------|--------|
| **kajweb/dict** | 有道+新东方官方分级，CET4/6/TOEFL/GRE 分类齐全，含音标/例句/短语 | zip 压缩包，需下载解析 | ★★★★★ |
| **ECDICT** | 77万词条离线 CSV，含音标/翻译/词频/词级 | 无考试分级，需自行匹配 | ★★★★ |
| **有道 API** | 实时查询，数据权威 | 无分级，有调用频率限制，需逐词请求 | ★★ |
| **手动整理** | 质量可控 | 耗时巨大，不现实 | ★ |

### 推荐：kajweb/dict 为主 + ECDICT 补充

**理由**：
- kajweb/dict 直接提供 TOEFL（9,213 词）和 GRE（7,199 词），无需额外分级
- 数据格式包含 `usphone`（美音音标）、`trans`（中文翻译）、`exam`（例题）
- ECDICT 作为补充，用于交叉验证和查缺补漏

## 三、架构改造

### 3.1 问题

当前所有词库数据内嵌在 `闪卡记忆.html` 中（14,000+ 行），已经导致 Trae Work 卡死。如果再加 7,000+ 词，文件将达到 20,000+ 行，问题会更严重。

### 3.2 改造方案：词库抽离

```
项目结构（改造后）
├── 闪卡记忆.html          # 主程序（~5,000 行，仅含逻辑+UI）
├── data/
│   ├── cet4.js           # CET4 词库（~4,500 词）
│   ├── cet6.js           # CET6 词库（~6,000 词）
│   ├── toefl.js          # TOEFL 词库（~4,000 词）
│   └── gre.js            # GRE 词库（~3,000 词）
├── index.html            # 入口页
└── assets/               # 静态资源
```

### 3.3 加载方式

```html
<!-- 在 闪卡记忆.html 中用 script 标签加载（兼容 file:// 协议） -->
<script src="data/cet4.js"></script>
<script src="data/cet6.js"></script>
<script src="data/toefl.js"></script>
<script src="data/gre.js"></script>
<script>
    // 词库数据挂载到全局变量
    const wordDB = {
        cet4: window.CET4_WORDS,
        cet6: window.CET6_WORDS,
        toefl: window.TOEFL_WORDS,
        gre: window.GRE_WORDS
    };
</script>
```

### 3.4 数据文件格式

每个 `data/*.js` 文件格式：

```javascript
// data/toefl.js
window.TOEFL_WORDS = [
    { en: 'abandon', cn: '放弃', phonetic: '/əˈbændən/', difficulty: 2 },
    // ...
];
```

### 3.5 Trae Work 索引排除

在 `.gitignore` 或 Trae Work 配置中排除 `data/` 目录，避免 IDE 索引大文件导致卡死。

## 四、补全策略

### 4.1 TOEFL（103 → 4,000）

**数据源**：kajweb/dict 的 `TOEFL_2.zip`（有道 TOEFL 9,213 词）

**策略**：
1. 下载并解析 zip，提取 `headWord`、`usphone`、`trans[].tranCn`
2. 按词频排序，取前 4,000 个（去除过于生僻的词）
3. 保留现有 103 词，去重后合并
4. difficulty 按词频分档：前 30% → 2，中间 40% → 3，后 30% → 4

### 4.2 GRE（108 → 3,000）

**数据源**：kajweb/dict 的 `GRE_2.zip`（有道 GRE 7,199 词）

**策略**：
1. 下载并解析 zip，提取 `headWord`、`usphone`、`trans[].tranCn`
2. 按词频排序，取前 3,000 个
3. 保留现有 108 词，去重后合并
4. difficulty 按词频分档：前 30% → 3，中间 40% → 4，后 30% → 5

### 4.3 CET4/CET6 查缺补漏

**数据源**：kajweb/dict 的 `CET4_2.zip`（3,739 词）和 `CET6_2.zip`（2,078 词）

**策略**：
1. 提取官方词表
2. 与现有词库对比，找出缺失词
3. 补入缺失词（预计 CET4 ~88 词，CET6 ~494 词）

## 五、实施步骤

### 第一步：下载并解析 kajweb/dict 数据（离线脚本）

```bash
# 下载词库 zip 文件
curl -L -o /tmp/TOEFL_2.zip https://github.com/kajweb/dict/raw/master/book/1521164640451_TOEFL_2.zip
curl -L -o /tmp/GRE_2.zip https://github.com/kajweb/dict/raw/master/book/1521164637271_GRE_2.zip

# 解压并解析 JSON
unzip /tmp/TOEFL_2.zip -d /tmp/toefl/
unzip /tmp/GRE_2.zip -d /tmp/gre/
```

### 第二步：编写转换脚本

```python
# scripts/convert_dict.py
import json, os, glob, re

def parse_zip_dir(zip_dir, output_file, var_name, max_words=None):
    """解析 kajweb/dict 的 JSON 文件，转换为项目格式"""
    words = []
    for json_file in sorted(glob.glob(f"{zip_dir}/*.json")):
        with open(json_file, 'r', encoding='utf-8') as f:
            data = json.load(f)
            for entry in data:
                head_word = entry.get('headWord', '')
                content = entry.get('content', {}).get('word', {}).get('content', {})
                # 音标
                phonetic = content.get('usphone', '')
                if phonetic:
                    phonetic = f'/{phonetic}/'
                # 中文翻译
                trans_list = content.get('trans', [])
                cn_parts = [t.get('tranCn', '').strip() for t in trans_list if t.get('tranCn')]
                cn = '；'.join(cn_parts[:3]) if cn_parts else ''
                
                if head_word and cn:
                    words.append({
                        'en': head_word,
                        'cn': cn,
                        'phonetic': phonetic,
                        'difficulty': 3  # 默认难度，后续按词频调整
                    })
    
    # 去重
    seen = set()
    unique = []
    for w in words:
        if w['en'] not in seen:
            seen.add(w['en'])
            unique.append(w)
    
    if max_words:
        unique = unique[:max_words]
    
    # 写入 JS 文件
    with open(output_file, 'w', encoding='utf-8') as f:
        f.write(f'window.{var_name} = [\n')
        for w in unique:
            f.write(f"    {{ en: '{w['en']}', cn: '{w['cn']}', phonetic: '{w['phonetic']}', difficulty: {w['difficulty']} }},\n")
        f.write('];\n')
    
    print(f"{var_name}: {len(unique)} words → {output_file}")

# 执行转换
parse_zip_dir('/tmp/toefl', 'data/toefl.js', 'TOEFL_WORDS', max_words=4000)
parse_zip_dir('/tmp/gre', 'data/gre.js', 'GRE_WORDS', max_words=3000)
```

### 第三步：抽离现有词库

1. 从 `闪卡记忆.html` 中提取 CET4（4412词）和 CET6（5506词）数据
2. 分别写入 `data/cet4.js` 和 `data/cet6.js`
3. 删除 HTML 中的内联词库数据（约 10,000 行）
4. 添加 `<script src="data/*.js">` 加载标签

```bash
# 提取脚本
node scripts/extract_words.js 闪卡记忆.html
# 输出: data/cet4.js, data/cet6.js
```

### 第四步：合并与去重

```python
# 合并现有词 + 新词，去重
def merge_words(existing_file, new_file, output_file, var_name):
    existing = load_js_array(existing_file)
    new_words = load_js_array(new_file)
    
    seen = {w['en'] for w in existing}
    for w in new_words:
        if w['en'] not in seen:
            existing.append(w)
            seen.add(w['en'])
    
    write_js_array(output_file, var_name, existing)
    print(f"{var_name}: {len(existing)} words (existing {len(existing)-len(new_words)} + new {len([w for w in new_words if w['en'] in seen])})")
```

### 第五步：HTML 改造

```html
<!-- 替换原来的内联 wordDB 定义 -->
<script src="data/cet4.js"></script>
<script src="data/cet6.js"></script>
<script src="data/toefl.js"></script>
<script src="data/gre.js"></script>
<script>
    const wordDB = {
        cet4: window.CET4_WORDS || [],
        cet6: window.CET6_WORDS || [],
        toefl: window.TOEFL_WORDS || [],
        gre: window.GRE_WORDS || []
    };
</script>
```

### 第六步：部署验证

1. 本地测试：`python3 -m http.server 8765`，验证所有词库可加载
2. 推送到 GitHub Pages + CloudBase
3. 验证线上词库数量正确

## 六、预期结果

| 词库 | 当前 | 补全后 | 增量 |
|------|------|--------|------|
| CET4 | 4,412 | ~4,500 | +88 |
| CET6 | 5,506 | ~6,000 | +494 |
| TOEFL | 103 | ~4,000 | +3,897 |
| GRE | 108 | ~3,000 | +2,892 |
| **合计** | **10,129** | **~17,500** | **+7,371** |

## 七、文件体积预估

| 文件 | 行数 | 大小 |
|------|------|------|
| 闪卡记忆.html（改造后） | ~5,000 | ~200KB |
| data/cet4.js | ~4,500 | ~350KB |
| data/cet6.js | ~6,000 | ~480KB |
| data/toefl.js | ~4,000 | ~320KB |
| data/gre.js | ~3,000 | ~250KB |
| **合计** | **~22,500** | **~1.6MB** |

**关键优势**：主 HTML 文件从 14,000+ 行降到 ~5,000 行，Trae Work 不再卡死。词库文件单独加载，可被浏览器缓存，且 IDE 可排除索引。

## 八、风险与应对

| 风险 | 应对 |
|------|------|
| kajweb/dict zip 下载失败 | 用 ECDICT 作为备选数据源 |
| 音标格式不一致 | 统一为 `/xxx/` 格式，用正则清洗 |
| 中文翻译过长 | 截取前 3 个释义，用分号连接 |
| 重复词 | 按 `en` 字段去重，保留现有词 |
| 文件加载顺序 | `<script>` 标签在 `wordDB` 定义之前 |
| file:// 协议加载 | `<script src>` 兼容 file://，无需 fetch |

## 九、执行顺序

```
1. 下载 kajweb/dict 的 TOEFL/GRE zip 文件          [10分钟]
2. 编写 Python 转换脚本，解析 JSON → JS            [30分钟]
3. 从 HTML 抽离 CET4/CET6 数据到 data/*.js         [15分钟]
4. 合并新词到 TOEFL/GRE 词库                       [10分钟]
5. 改造 HTML：删除内联数据，添加 script 加载       [15分钟]
6. 本地测试验证                                   [10分钟]
7. 推送到 GitHub Pages + CloudBase                [10分钟]
                                                   --------
                                                   总计 ~100分钟
```