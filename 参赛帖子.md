# 【学习工作赛道】闪卡记忆 — 用 FSRS 算法 + 消除游戏把背单词做成上瘾的事

> 标签：`学习工作`
> 体验地址：https://project123-carl-d5f16u165be3d97a-1341005541.tcloudbaseapp.com/

---

## 一、Demo 简介

**闪卡记忆** 是一个把「科学记忆算法」和「消除游戏」融合在一起的英语单词学习 Web App。手机、电脑浏览器打开即用，无需下载安装。

**面向谁**：备考四六级、托福、GRE 的学生，以及任何想系统性扩充英语词汇量的人。

**核心功能**：

### 1. 四大词库,共 26,410 词,真正够用

| 词库 | 词数 | 适用人群 |
|------|------|----------|
| CET4 四级 | 4,412 | 基础必备 |
| CET6 六级 | 5,506 | 进阶提升 |
| TOEFL 托福 | 9,280 | 留学必备 |
| GRE | 7,212 | 挑战高难 |

市面上很多背单词 App 只给几百词的"体验版",而我们做到了**完整词库**——托福、GRE 都是从有道官方词库补全到几千词量级,够你从入门到考完。

> 📷 *[截图位置:首页四个词库卡片 + 今日新词数设置]*

### 2. FSRS-4 科学记忆算法,比艾宾浩斯更精准

不是简单的"每天复习一次",而是基于 **FSRS-4 三因子记忆模型**:

- **D (Difficulty 难度)**:这个词对你有多难
- **S (Stability 稳定性)**:记忆能维持多久
- **R (Retrievability 可回忆性)**:此刻你还能想起来的概率

核心公式:`R = (1 + t/(9S))^(-1)`

每次你点击"认识/不认识",算法都会实时更新这三个参数,**动态调整**这个词的下次复习时间。难词多见,易词少扰——**比传统艾宾浩斯曲线更贴合个人记忆规律**。

### 3. 微循环学习:闪卡 + 消除游戏双闭环

这是产品最核心的设计。不是"先背完再玩",而是**学习和游戏交替进行**:

```
学 4 个新词(闪卡正反翻转)
    ↓
进入消除游戏检测(把刚学的词和释义配对消除)
    ↓
游戏结果回写记忆系统(消除快=掌握好,犹豫=需复习)
    ↓
下一组 4 词(含未掌握的重学词)
    ↓
循环直到今日任务完成
```

**关键点**:游戏不只是娱乐,游戏里的每一次配对、每一次犹豫,都会通过统一的 `MemorySystem.rate(word, outcome)` 接口反馈给记忆算法,影响这个词的 D/S/R 参数。**玩得好,这个词下次复习就晚一点;玩砸了,明天再战。**

### 4. 单词本:你的个人词库全景

底部导航栏的「单词本」Tab,可以浏览任意词库的全部单词,支持三种排序:

- **按词频**:高频词优先,学了就能用上
- **A-Z**:字典式查阅
- **按掌握度**:一眼看清哪些词还不熟

每个单词都能看到你已经学了多少次、复习了多少次,学习进度可视化。

> 📷 *[截图位置:单词本页面,显示词频排序 + 掌握度圆点]*

### 5. Duolingo 式游戏化体验

- **金币 / 宝石**双货币系统,完成任务、游戏通关获得奖励
- **连续学习天数**激励,断签就清零
- **道具系统**:提示、洗牌、时间加成,卡关时可以救场
- **Duolingo 风格 UI**:实心偏移阴影、高饱和色彩、大圆角、粗体字,20 个定制图标替换了原本的 emoji,视觉质感统一

> 📷 *[截图位置:游戏中状态 + 顶部金币宝石统计]*

---

## 二、Demo 创作思路

### 灵感来源

我自己就是背单词的重度用户,试过市面上几乎所有主流 App。一个很真实的感受是:**大家都在做"科学复习",但没人真正解决"无聊"这个问题**。

- 有的 App 纯闪卡,翻来覆去就是正反两面,翻两天就腻了
- 有的 App 加了小游戏,但游戏归游戏、学习归学习,**游戏打再多分,该忘的词还是忘**
- 还有的 App 把算法包装得很玄,但用户根本感受不到"我学得更好了"

我就想:**能不能把游戏的每一次操作,真正变成记忆算法的输入?** 玩得爽的同时,记忆数据也在被科学地更新。

### 想解决的问题

1. **背单词太无聊,坚持不下去** —— 用消除游戏的即时反馈制造上瘾感
2. **游戏和学习脱节** —— 游戏结果直接回写 FSRS 算法,玩即是学
3. **记忆算法是个黑盒,用户没感知** —— 单词本里能看到每个词的掌握度、学习次数,算法透明
4. **词库不完整** —— 托福、GRE 都补到了几千词,不是"体验版"

### 为什么做这个方向

- **刚需高频**:中国每年几千万学生备考英语,背单词是铁需求
- **技术有深度**:FSRS 算法本身有学术基础(Duolingo、Anki 都在用类似思路),值得做精
- **能体现 TRAE 的能力**:涉及算法实现、大数据处理(2.6 万词)、架构重构、自动部署,过程丰富,适合展示 AI 辅助开发的全流程

---

## 三、Demo 体验地址

**在线体验(推荐,手机浏览器打开效果最佳)**:

- 🌐 CloudBase(国内访问快):https://project123-carl-d5f16u165be3d97a-1341005541.tcloudbaseapp.com/
- 🌐 GitHub Pages:https://kaigeer344-coder.github.io/flashcard-memory/

**怎么体验**:
1. 打开链接,选择一个词库(建议先选四级体验)
2. 设置今日新词数(默认 10 个)
3. 点「开始今日任务」,先学 4 个新词(点卡片翻转看释义)
4. 学完进入消除游戏,把单词和释义配对消除
5. 游戏结束后继续下一组,直到今日任务完成
6. 点底部「单词本」可以查看所有单词的掌握情况
7. 点底部「统计」看学习数据

---

## 四、TRAE 实践过程

整个产品**从 0 到 1 全程用 TRAE 开发完成**,包括算法实现、架构重构、词库补全、UI 设计、自动部署。下面是几个关键阶段。

### 阶段 1:FSRS-4 记忆算法实现

这是我最先攻克的核心。让 TRAE 帮我实现了三因子记忆模型:

- **D (难度)**:每次"不认识"会提升难度,有上限
- **S (稳定性)**:综合难度和上次回忆结果更新,决定记忆能维持多久
- **R (可回忆性)**:用公式 `R = (1 + t/(9S))^(-1)` 实时计算此刻还能想起来的概率

关键设计:统一记忆接口 `MemorySystem.rate(word, outcome)`,无论是闪卡的"认识/不认识",还是游戏里的"配对成功/超时未配",都走同一个评分入口,保证记忆数据一致性。

> 📷 *[截图1:TRAE 中实现 FSRS 算法的对话,展示 rate 函数和 D/S/R 更新逻辑]*
> Session ID:`[在此填入 Session ID 1]`

### 阶段 2:微循环学习流程

把闪卡和消除游戏串成闭环,这是产品最核心的流程设计:

- 每组 4 个新词,闪卡学完进游戏检测
- 游戏结果通过 `MemorySystem.rate()` 回写算法
- 下一组自动加入未掌握的重学词
- `endGame` 超时分支要把超时词推入 `sessionReviewQueue`,确保重新进入学习循环(这个坑踩过,超时词会漏掉)

> 📷 *[截图2:TRAE 中实现微循环流程的对话,展示 buildTodayQueue 和游戏闭环逻辑]*
> Session ID:`[在此填入 Session ID 2]`

### 阶段 3:词库补全 — 从 1 万词到 2.6 万词

这是工作量最大的一步。原本托福只有 103 词、GRE 只有 108 词,严重不够用。

**数据源**:kajweb/dict(有道 TOEFL/GRE 官方词库)

**过程**:
1. 让 TRAE 写 Node.js 脚本,从数据源下载并解析 TOEFL(9,213 词)和 GRE(7,199 词)
2. 和原有词库合并去重,TOEFL 增至 9,280 词,GRE 增至 7,212 词
3. 踩坑:正则 `en: '([^']*)'` 无法匹配含单引号的翻译,改用 `eval()` 方式才完整提取(CET4 从 4,391 恢复到 4,412)
4. 踩坑:GRE 数组提取边界 `greStart + 4` 应为 `greStart + 5`,重写为括号深度追踪函数

最终词库:四级 4,412 + 六级 5,506 + 托福 9,280 + GRE 7,212 = **26,410 词**。

> 📷 *[截图3:TRAE 中词库补全的对话,展示 Node.js 脚本和合并去重逻辑]*
> Session ID:`[在此填入 Session ID 3]`

### 阶段 4:架构重构 — 解决 Trae Work 卡死问题

词库补全后,`闪卡记忆.html` 膨胀到 **15,355 行**,Trae Work 打开和保存时直接卡死。

**解决方案**:把词库数据从 HTML 内联抽离到 `data/` 目录的独立 JS 文件:

```
data/
├── cet4.js   (4412词, 351KB)
├── cet6.js   (5506词, 441KB)
├── toefl.js  (9280词, 820KB)
└── gre.js    (7212词, 695KB)
```

通过 `<script src>` 加载,兼容 `file://` 协议。HTML 从 15,355 行精简到 5,226 行,**卡死问题彻底解决**。

### 阶段 5:性能优化

- **预加载缓存**:所有词的记忆数据在初始加载时批量预读到一个 Map 缓存,避免 N+1 的 localStorage 查询(原来每个词查一次,主线程直接卡死)
- **增量更新**:`saveWordMemory` 只更新缓存中的对应词,不做全量失效
- **分页渲染**:单词本里 TOEFL 9,280 词一次性渲染会卡,改为每页 200 词分页加载
- **O(N) 优化**:`useHint` 用 Map 分组替代嵌套循环;`buildTodayQueue` 用单次 for 循环分组替代多次 filter/sort
- **批量写入**:游戏结果在 `endGame` 时一次性写入 localStorage,而不是每答对一题就存一次

### 阶段 6:CloudBase 自动部署

用 GitHub Actions + CloudBase CLI 搭了自动部署流水线:

- 推送到 main 分支自动触发
- 安装 CloudBase CLI → 用 API Key 登录 → 上传 `index.html`、`闪卡记忆.html`、`assets/`、`data/` 到静态托管
- CDN 几分钟内刷新

配置了 `TCB_ENV_ID` 和 `TCB_API_KEY` 两个 GitHub Secrets,之后每次 push 代码,国内用户访问的 CloudBase 站点自动更新。

### 阶段 7:UI 图标化

最后一步,把全 App 的 emoji 图标替换成 20 个定制 PNG 图标(导航栏、统计、商店、连续学习、引导页、VIP、道具、货币等),视觉质感从"Demo 感"升级到"产品感"。配套写了图标尺寸 CSS,保证不同位置图标大小统一。

---

## 五、技术栈一览

| 层 | 技术 |
|----|------|
| 前端 | 原生 HTML/CSS/JavaScript(无框架,纯 Web) |
| 数据存储 | localStorage(批量读写) |
| 记忆算法 | FSRS-4 三因子模型 |
| 词库数据 | 独立 JS 文件(`<script src>` 加载) |
| 部署 | GitHub Pages + 腾讯云 CloudBase |
| CI/CD | GitHub Actions 自动部署 |

---

## 六、开发心得

### 1. AI 辅助开发,不是"让 AI 写完拉倒"

整个过程里,TRAE 帮我写了大量代码,但**架构决策、产品逻辑、踩坑排查都是我来主导**。比如:
- FSRS 算法的三因子模型,是我先理解了原理,再让 TRAE 实现
- 词库抽离到 `data/` 目录,是我发现 Trae Work 卡死后主动决定的架构方向
- `endGame` 超时词漏掉的问题,是我测试时发现并定位的

AI 是放大器,不是替代品。你越清楚要什么,AI 给你的产出越精准。

### 2. 踩坑是最好的展示素材

- 正则匹配含单引号的翻译失败 → 改用 eval 提取
- GRE 数组边界 off-by-one → 重写括号深度追踪
- N+1 localStorage 查询卡死主线程 → 改为预加载 Map 缓存
- GitHub Pages 首次部署瞬时失败 → 通过 Pages API 重触发

每个坑都让产品更稳,也让 TRAE 的开发过程更真实、更有说服力。

### 3. 不要追求完美,要追求"能跑通且真实"

初赛不拼复杂度,拼真实价值。我的产品不是最炫的,但每一个功能都跑通了、能体验、有数据支撑——这就够了。

---

## 七、后续规划

- **语音发音**:接入 TTS,点单词能读出来
- **错词本**:自动收集高频错误词,集中突破
- **学习日历**:可视化每天的学习量,像 GitHub 贡献图
- **词根词缀**:拆解单词结构,帮助记忆

---

**报名帖链接**:[在此填入你通过的社区报名帖链接]

**体验地址**:
- CloudBase:https://project123-carl-d5f16u165be3d97a-1341005541.tcloudbaseapp.com/
- GitHub Pages:https://kaigeer344-coder.github.io/flashcard-memory/

**GitHub 仓库**:https://github.com/kaigeer344-coder/flashcard-memory

---

> 📌 **发帖前需要你补充的内容**:
> 1. 3 个 Session ID(双击 TRAE 对话复制)
> 2. 至少 3 张开发关键步骤截图
> 3. 通过的社区报名帖链接
> 4. (可选)几张产品界面截图,配在功能介绍里
